DSL for the configuration of a single argument (value).
Not all command apply to all argument classes.

description TEXT

	Help text for the argument or flag.
	Defaults to the argument name itself, plus the validation command.

	Applies to:		input, optional, splat, flag
	Does not apply to:	invisible

hidden
	When present hide the argument or flag from the help.
	Default: Not hidden.

	Applies to:		input, optional, splat, flag
	Does not apply to:	invisible

interactive ?TEXT?

	When present the argument can ask interactively for its value.
	The text is for the interactive prompt.
	Without text a default text is made, based on the argument
	name and validation.

	Applies to:		input, optional
	Does not apply to:	splat, flag, invisible

default DATA

	The raw default value for the argument or flag.
	When used this value goes through regular validation.

	Applies to:		input, optional, flag, invisible
	Does not apply to:	splat

generate CMD

	 Command prefix executed to generate the value (*) of the
	 argument or flag when requested. The details of its
	 interaction with

	 	default, interactive

	are expained in a separate document. Note, the result is the
	internal representation of the value, and thus does not have
	to be, actually must not be, validated.

	Applies to:		input, optional, flag, invisible
	Mandatory to:		invisible
	Does not apply to:	splat

validate CMD

	Command prefix executed to validate user input (string), and
	return the canonical internal representation of said input.

	Applies to:		input, optional, flag, splat
	Does not apply to:	invisible

on CMD

	Command prefix executed when the internal value of the
	argument or flag is defined.

	Applies to:	input, optional, flag, splat, invisible


-- skip -- typecheck - allow rejection of value, pass to next
